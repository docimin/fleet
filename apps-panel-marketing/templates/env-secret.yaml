{{- if not .Values.externalSecret.enabled }}
# envFrom in the Deployment needs this secret to exist. Marketing config is
# baked at build time (VITE_*), so it is empty until/unless ExternalSecret is on.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.name }}
type: Opaque
{{- end }}
