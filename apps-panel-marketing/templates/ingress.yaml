{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}-ingress
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
    {{- $mw := list }}
    {{- if .Values.httpsRedirect.enabled }}
    {{- $mw = append $mw (printf "%s-%s-redirect-https@kubernetescrd" .Release.Namespace .Values.name) }}
    {{- end }}
    {{- if .Values.basicAuth.enabled }}
    {{- $mw = append $mw (printf "%s-%s-basicauth@kubernetescrd" .Release.Namespace .Values.name) }}
    {{- end }}
    {{- if $mw }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ join "," $mw }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $.Values.name }}-service
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
{{- end }}
