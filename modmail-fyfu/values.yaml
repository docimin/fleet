replicaCount: 1

name: fyfu-fayevr-dev

image:
  repository: harbor.fayevr.dev/fleet/modmail-bot
  tag: "fyfu"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "traefik"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  hosts:
    - host: fyfu.fayevr.dev
      paths:
        - path: "/"
          pathType: Prefix
  tls:
    - hosts:
        - fyfu.fayevr.dev
      secretName: fyfu-fayevr-dev-tls-secret

secrets:
  name: fyfu-fayevr-dev-variables
  imagePullSecrets:
    - name: harbor-secret

resources:
  limits:
    memory: "512Mi"
    cpu: "500m"
  requests:
    memory: "256Mi"
    cpu: "100m"

probes:
  liveness:
    initialDelaySeconds: 60
    periodSeconds: 15
    timeoutSeconds: 5
    failureThreshold: 3
    successThreshold: 1
  readiness:
    initialDelaySeconds: 30
    periodSeconds: 15
    timeoutSeconds: 5
    failureThreshold: 3
    successThreshold: 1

pdb:
  enabled: false
  minAvailable: 1

externalSecret:
  enabled: true
  store: vault
  vaultPath: modmail-fyfu
  refreshInterval: 1h
