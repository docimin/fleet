{{- if .Values.httpsRedirect.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Values.name }}-redirect-https
spec:
  redirectScheme:
    scheme: https
    permanent: true
{{- end }}
